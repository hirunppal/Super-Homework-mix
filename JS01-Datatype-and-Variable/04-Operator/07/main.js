let numa = Number(prompt("give me first number"));
let numb = Number(prompt("give me sec number"));

function sum(numa, numb) {
  let summary = numa + numb;
  return summary;
}

const total = sum(numa, numb);
alert(total);
console.log(total);
