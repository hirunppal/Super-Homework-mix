// บรรทัด \* ให้ผลลัพธ์เป็นอะไร เพราะอะไร

// ```js
// function sayHi(name) {
//   let name = "Guest";
//   console.log(name); // *
// }

// sayHi("Jim");

// ```
function sayHi(name) {
  //   let name = "Guest";
  console.log(name); //
}

sayHi("Jim");

// ให้ผลลัพท์เป็น error` function say hi (name) ถือเป็นการประกาศตัวแปล
// การ let name อีกครั้งทำให้ เกิด error doble declare
